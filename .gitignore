.DS_Store
.env
.env.*
!.env.example
.claude/
.agent/
.codex/
.cursor/
.vscode/
.idea/
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/

node_modules/
dist/
build/
*.tsbuildinfo
.vite/
frontend/vite.config.js
frontend/vite.config.d.ts

tmp/
local_artifacts/
.cab/
.langgraph/
.cache/
logs/
coverage/

*.mp4
*.mov
*.m4v
*.avi
*.mkv
*.mp3
*.wav
*.aac

# Stage list covers = drama keyframes (local demo only; not shipped to the public repo)
/assets/public/assets/covers/

provider_trace*.jsonl
*_trace_redacted.jsonl
*.sqlite
*.db
*.log
*.zip
*.tar
*.tgz
*.gz
*.apk
*.aab
*.ipa
*.pem
*.key
*.p12
*.crt
*.cer
credentials.json
secrets.json
secret.json
*.credentials.json
*.secret.json

frontend/android/**/build/
frontend/android/.gradle/
frontend/android/local.properties
frontend/android/app/src/main/assets/public/

# Internal dev docs / process — kept local, not part of the public product repo
/CLAUDE.md
/REPO_STRUCTURE.md
/PUBLICATION_REVIEW.md
/docs/
/data/evals/
# /data/review/ stays local EXCEPT the load-bearing production-graph inputs below (P0-B). Git cannot
# re-include a file once its parent DIR is excluded, so ignore the review CONTENTS (/*) rather than the
# dir itself, then re-include the committed inputs the context-grounding loaders are fail-closed on.
# See docs/context/agentic-production-graph-contract.md P0-B.
/data/review/*
!/data/review/context_memory/
/data/review/context_memory/*
!/data/review/context_memory/*.v0.1.json
!/data/review/drama_synopses.v0.1.json

# Studio console covers = drama keyframes (local demo only)
/studio/assets/covers/

# Studio console rolling sandbox drama (regenerated by /api/studio/promote; local demo only)
/data/dramas/studio_sandbox/

# Producer local media-path sidecar (keeps machine paths out of the public registry; local only)
/data/dramas/*/media_local.v0.1.json

# auto-generated Studio upload dramas (drama_<random>) are throwaway local artifacts — never commit
/data/dramas/drama_*/
/data/review/context_memory/drama_*.v0.1.json
